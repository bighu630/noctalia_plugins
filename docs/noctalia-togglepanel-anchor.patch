diff --git a/src/shell/bar/bar.cpp b/src/shell/bar/bar.cpp
index 5e0948e..5d12041 100644
--- a/src/shell/bar/bar.cpp
+++ b/src/shell/bar/bar.cpp
@@ -3334,6 +3334,24 @@ bool Bar::onPointerEvent(const PointerEvent& event) {
     case PointerEvent::Type::Motion:
     case PointerEvent::Type::Button:
     case PointerEvent::Type::Axis:
+      if (event.type == PointerEvent::Type::Button && event.pressed) {
+        // 记录最近一次 bar 按钮按下位置（output 逻辑坐标）：Lua 的
+        // noctalia.togglePanel 无锚点 API，打开面板时用它作 open-near-click
+        // 锚点，让插件面板（如全局菜单）出现在触发按钮附近。
+        if (targetInstance != nullptr && targetInstance->output != nullptr) {
+          float anchorX = static_cast<float>(event.sx);
+          float anchorY = static_cast<float>(event.sy);
+          if (m_platform != nullptr) {
+            if (const auto* out = m_platform->findOutputByWl(targetInstance->output);
+                out != nullptr && out->hasUsableGeometry()) {
+              const auto [surfaceX, surfaceY] = surfaceOriginForOutputLocal(*targetInstance, *out);
+              anchorX += surfaceX;
+              anchorY += surfaceY;
+            }
+          }
+          PanelManager::instance().setLastClickAnchor(targetInstance->output, anchorX, anchorY);
+        }
+      }
       if (event.type == PointerEvent::Type::Button && event.button == BTN_RIGHT && event.pressed) {
         const auto sx = static_cast<float>(event.sx);
         const auto sy = static_cast<float>(event.sy);
diff --git a/src/shell/panel/panel_manager.cpp b/src/shell/panel/panel_manager.cpp
index 080278c..adedee1 100644
--- a/src/shell/panel/panel_manager.cpp
+++ b/src/shell/panel/panel_manager.cpp
@@ -640,8 +640,10 @@ void PanelManager::openPanel(const std::string& panelId, PanelOpenRequest reques
       && barConfig.reserveSpace
       && barConfig.thickness > 0;
   const auto marginLeftFromAnchor = clampMargin(
-      request.anchorX - static_cast<float>(panelWidth) * 0.5f, static_cast<std::int32_t>(panelWidth), outputWidth,
-      screenPadding
+      request.anchorLeftAligned
+          ? request.anchorX
+          : request.anchorX - static_cast<float>(panelWidth) * 0.5f,
+      static_cast<std::int32_t>(panelWidth), outputWidth, screenPadding
   );
   const auto marginTopFromAnchor = clampMargin(
       request.anchorY - static_cast<float>(panelHeight) * 0.5f, static_cast<std::int32_t>(panelHeight), outputHeight,
@@ -1419,6 +1421,13 @@ void PanelManager::togglePanel(const std::string& panelId, PanelOpenRequest requ
   }
 }
 
+void PanelManager::setLastClickAnchor(wl_output* output, float x, float y) {
+  m_lastClickAnchorOutput = output;
+  m_lastClickAnchorX = x;
+  m_lastClickAnchorY = y;
+  m_hasLastClickAnchor = true;
+}
+
 void PanelManager::togglePanel(const std::string& panelId) {
   if (m_persistentHost.hasPanel(panelId)) {
     if (m_persistentHost.isOpen(panelId)) {
@@ -1432,8 +1441,19 @@ void PanelManager::togglePanel(const std::string& panelId) {
     closePanel();
     return;
   }
-  // Output left unset: openPanel resolves it (focus source, else compositor probe).
-  openPanel(panelId, PanelOpenRequest{});
+  // Lua noctalia.togglePanel 无锚点 API：用最近一次 bar 按钮点击位置作为
+  // open-near-click 锚点（bar 层在按钮按下时调用 setLastClickAnchor）。
+  PanelOpenRequest request;
+  if (m_hasLastClickAnchor) {
+    request.output = m_lastClickAnchorOutput;
+    request.anchorX = m_lastClickAnchorX;
+    request.anchorY = m_lastClickAnchorY;
+    request.hasAnchorPosition = true;
+    // Lua 触发的面板（如全局菜单）左边缘对齐点击位置（macOS 下拉形态）
+    request.anchorLeftAligned = true;
+  }
+  // Output left unset (no anchor): openPanel resolves it (focus source, else probe).
+  openPanel(panelId, std::move(request));
 }
 
 bool PanelManager::onPointerEvent(const PointerEvent& event) {
diff --git a/src/shell/panel/panel_manager.h b/src/shell/panel/panel_manager.h
index 68cc279..b91b8f4 100644
--- a/src/shell/panel/panel_manager.h
+++ b/src/shell/panel/panel_manager.h
@@ -42,6 +42,9 @@ struct PanelOpenRequest {
   float anchorY = 0.0f;
   bool hasExplicitAnchor = false;
   bool hasAnchorPosition = false;
+  // true: 面板左边缘对齐锚点 x（Lua togglePanel 场景，macOS 菜单下拉）；
+  // false: 面板水平居中于锚点（bar action 内置面板保持原行为）。
+  bool anchorLeftAligned = false;
   std::string_view context;
   std::string_view sourceBarName;
 };
@@ -106,6 +109,10 @@ public:
   bool onPointerEvent(const PointerEvent& event);
   void onKeyboardEvent(const KeyboardEvent& event);
 
+  /// 记录最近一次 bar 按钮按下位置（output 逻辑坐标），供无锚点 togglePanel
+  /// （Lua noctalia.togglePanel）打开面板时作为 open-near-click 锚点。
+  void setLastClickAnchor(wl_output* output, float x, float y);
+
   [[nodiscard]] bool isOpen() const noexcept;
   [[nodiscard]] bool isOpenPanel(std::string_view panelId) const noexcept;
   [[nodiscard]] bool isPanelTransitionActive() const noexcept;
@@ -211,6 +218,12 @@ private:
   std::function<bool(wl_output*, std::string_view)> m_attachedPanelBarSettledCallback;
   PanelClickShield m_clickShield;
   PersistentPanelHost m_persistentHost;
+
+  // 最近一次 bar 按钮点击锚点（Lua togglePanel 无锚点 API，用 bar 指针位置兜底）
+  wl_output* m_lastClickAnchorOutput = nullptr;
+  float m_lastClickAnchorX = 0.0f;
+  float m_lastClickAnchorY = 0.0f;
+  bool m_hasLastClickAnchor = false;
   std::unique_ptr<FocusGrab> m_focusGrab;
 
   std::unique_ptr<Surface> m_surface;

# ============================================================
# 应用说明（v1+v2 完整版）
# ============================================================
# 让 noctalia.togglePanel(id)（Lua）打开面板时使用"最近一次 bar 按钮
# 点击位置"作为 open-near-click 锚点，且面板**左边缘对齐点击位置**
# （macOS 下拉形态）；bar action 内置面板（控制中心等）保持居中。
#
# 在 noctalia 源码仓库根目录应用：
#   git apply /path/to/noctalia-togglepanel-anchor.patch
# 然后构建安装（meson/just），重启 Noctalia。
#
# 改动（3 个文件）：
#   1. src/shell/bar/bar.cpp            — bar 按钮按下时记录点击位置（output 坐标）
#   2. src/shell/panel/panel_manager.h  — setLastClickAnchor 声明 + 锚点成员 + anchorLeftAligned
#   3. src/shell/panel/panel_manager.cpp— 无锚点 togglePanel 用最近点击做锚点（左对齐）
#
# 已在 v1 基础上构建过的：手动补 v2 三处即可（增量 ninja 只编译 2 个文件）：
#   a. panel_manager.h  PanelOpenRequest 加: bool anchorLeftAligned = false;
#   b. panel_manager.cpp marginLeftFromAnchor 改为三目（anchorLeftAligned ? anchorX : anchorX - panelW/2）
#   c. panel_manager.cpp togglePanel 无锚点分支加: request.anchorLeftAligned = true;
#
# 插件侧配合（生效后）：global-menu/plugin.toml 的 [[panel]] 已配置
#   position = "auto" + open_near_click = true（无需再改）。
